# MCP Switcher - Установка и настройка

## 🚀 Быстрая установка

### 1. Сборка релиза
```bash
cd /Volumes/External/Code/McpSwitcher
swift build --configuration release
```

### 2. Настройка автозагрузки
```bash
# Запуск скрипта управления
./manage-tray.sh enable
```

### 3. Проверка работы
```bash
./manage-tray.sh status
```

## 📱 Использование

### GUI (Tray приложение)
- Иконка в статус-баре macOS
- Левый клик - открыть меню серверов
- Правый клик - контекстное меню

### CLI (Командная строка)
```bash
# Показать все серверы
./.build/release/mcp-switcher list

# Включить сервер
./.build/release/mcp-switcher enable server-name

# Отключить сервер
./.build/release/mcp-switcher disable server-name

# Переключить состояние
./.build/release/mcp-switcher toggle server-name
```

## 🔧 Управление автозагрузкой

### Скрипт управления
```bash
./manage-tray.sh [command]
```

Доступные команды:
- `start` - Запустить немедленно
- `stop` - Остановить
- `restart` - Перезапустить
- `status` - Показать статус
- `enable` - Включить автозагрузку
- `disable` - Отключить автозагрузку

### Ручное управление
```bash
# Загрузить агент
launchctl load ~/Library/LaunchAgents/com.mcpswitcher.tray.plist

# Выгрузить агент
launchctl unload ~/Library/LaunchAgents/com.mcpswitcher.tray.plist

# Проверить статус
launchctl list | grep mcp
```

## 📂 Структура файлов

```
/Volumes/External/Code/McpSwitcher/
├── .build/release/           # Релизные бинарники
│   ├── mcp-switcher         # CLI инструмент
│   └── mcp-tray            # GUI приложение
├── manage-tray.sh          # Скрипт управления автозагрузкой
└── ~/Library/LaunchAgents/
    └── com.mcpswitcher.tray.plist  # Launch Agent
```

## 🔄 Синхронизация с mcp.json

Приложение автоматически синхронизирует включенные серверы с файлом `~/.cursor/mcp.json`:

- ✅ Включение сервера → добавляется в mcp.json
- ✅ Отключение сервера → удаляется из mcp.json
- ✅ Файл всегда содержит только активные серверы

## 📊 Мониторинг

### Логи приложения
```bash
# Логи вывода
tail -f /tmp/mcp-tray.out.log

# Логи ошибок
tail -f /tmp/mcp-tray.err.log
```

### Статус базы данных
```bash
ls -la ~/Library/Application\ Support/McpSwitcher/
```

## 🛠️ Troubleshooting

### Приложение не запускается
```bash
# Проверить логи
./manage-tray.sh status
tail -f /tmp/mcp-tray.err.log

# Перезапустить
./manage-tray.sh restart
```

### Иконка не отображается в статус-баре
```bash
# Перезагрузить статус-бар
killall SystemUIServer
```

### Проблемы с синхронизацией mcp.json
```bash
# Проверить права доступа
ls -la ~/.cursor/mcp.json

# Резервная копия
cp ~/.cursor/mcp.json ~/.cursor/mcp.json.backup
```

## 🎯 Автозагрузка при входе в систему

Launch Agent автоматически запускает приложение при входе пользователя в систему и перезапускает его при сбое. Приложение работает в фоне и потребляет минимальные ресурсы системы.
